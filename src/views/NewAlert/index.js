import React from 'react';

export default class NewAlertView extends React.Component {
  constructor(props) {
    super(props);

    this.state = {
    };
  }

  render() {
    return (
      <div>New Alert</div>
    );
  }
}